pipeline {
    agent any

    environment {
        AUTHOR = "Reiya Tenggara"
        USERNAME = "reiya24"
    }

    stages {

        stage("environment") {
            steps{
                echo("autor = ${AUTHOR}")
                echo("username = ${USERNAME}")
            }
        }

    }
}